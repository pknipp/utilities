{% extends "layout.html" %}

{% block content %}
<div class="text-center">
    List of utilities provided by this API:
    <ul class="text-center">
        {% for utility in utilities %}
            <li>{{ utility }}</li>
        {% endfor %}
    </ul>
</div>

{% endblock %}
